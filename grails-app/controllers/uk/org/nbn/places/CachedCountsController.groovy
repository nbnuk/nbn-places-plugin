package uk.org.nbn.places

import uk.org.nbn.places.CachedCount

class CachedCountsController {
    def bieService
    def biocacheService

    def weeklyDigest(placeId){
        //fetch all the rows for a given placeId and return them
    }
}
